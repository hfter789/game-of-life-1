--add mouseclick functionality to conway so that when the plot is clicked, it can generate 		an alived pixel.
	--onclick function is invoked after the program terminates in pdb
		SOLUTION: use plt.pause() to allow program to update, it will call sleep if no event is waiting.